language: node_js
dist: trusty
sudo: required
node_js: stable
cache:
  directories:
    - node_modules
addons:
  firefox: latest
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
cache:
  directories:
    - node_modules
install:
  - npm install -g polymer-cli
  - polymer install --variants
script:
  - xvfb-run polymer test
